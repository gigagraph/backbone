# Cue

[`cue` CLI](../../../system-setup/toolchains/cue/README.md) includes an implementation of LSP server for `cue`. This setup recommends using it as an implementaiton of LSP server for `cue`.

## Installation

Follow the [installation instructions for the `cue` toolchain](../../../system-setup/toolchains/cue/README.md#installation).
